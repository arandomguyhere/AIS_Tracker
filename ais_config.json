{
  "sources": {
    "aisstream": {
      "enabled": true,
      "api_key": "${AISSTREAM_API_KEY}",
      "description": "Real-time WebSocket AIS data from aisstream.io (requires free API key)"
    },
    "aishub": {
      "enabled": false,
      "username": "${AISHUB_USERNAME}",
      "description": "Community AIS data sharing from aishub.net (free, requires registration and data contribution)"
    },
    "marinesia": {
      "enabled": true,
      "api_key": "${MARINESIA_API_KEY}",
      "rate_limit": 30,
      "description": "REST API fallback from marinesia.com (free, no key required for basic access)"
    },
    "gfw": {
      "enabled": true,
      "api_key": "${GFW_API_KEY}",
      "rate_limit": 10,
      "description": "Global Fishing Watch enrichment data (requires free API key, enrichment only)"
    }
  },
  "priority": [
    "aisstream",
    "aishub",
    "marinesia"
  ],
  "poll_interval": 60,
  "position_cache_ttl": 300,
  "area_tracking": {
    "enabled": true,
    "auto_update": true,
    "bounding_box": {
      "lat_min": 53.0,
      "lon_min": 9.0,
      "lat_max": 66.0,
      "lon_max": 30.0,
      "description": "Baltic Sea - Cable Infrastructure Monitoring Zone"
    }
  },
  "notes": {
    "env_vars": [
      "AISSTREAM_API_KEY - Get from https://aisstream.io/",
      "AISHUB_USERNAME - Register at https://www.aishub.net/register",
      "MARINESIA_API_KEY - Get from https://marinesia.com/",
      "GFW_API_KEY - Get from https://globalfishingwatch.org/our-apis/"
    ],
    "priority_explanation": "Sources are tried in order. AISStream provides real-time WebSocket data. AISHub is community-based fallback. Marinesia is REST fallback.",
    "area_tracking_note": "Set area_tracking.enabled=true to receive ALL vessels in the bounding box instead of only watchlisted vessels"
  }
}